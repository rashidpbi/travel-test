'use client'
import React from 'react'

const ProductCard = () => {
  return (
    <div>
      ProductCard
      <div onClick={()=>{console.log("prodcut added")}}>add</div>
    </div>
  )
}

export default ProductCard
