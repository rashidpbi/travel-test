{
  "name": "my-monorepo",
  "version": "1.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "workspaces": ["packages/*", "apps/*"],
  "devDependencies": {
    "@travel/eslint-config":"*",
    "turbo":"latest"
  },
  "packageManager": "pnpm@8.15.4",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0",
    "npm": "not supported"
  }
}
